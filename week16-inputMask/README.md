# 🧩 City AutoComplete App

An autoComplete search app built with **React JS** and **TailwindCSS**. Type a few letters, and instantly get city name suggestions from a local JSON dataset.  

## 🚀 Features

- ⚡ **Real-time suggestions** — instantly filters city names as you type.  
- 🎯 **Case-sensitive matching** — strict comparison for better control.
- 📜 **Local dataset** — uses `cities.json` for offline-friendly data.  
- 🧠 **Smart filtering logic** — only displays relevant results dynamically.  
- 🖱️ **Keyboard & click support** — use `Tab` to autocomplete or click to select.

## 🧠 Project Structure
```
├── src/
│ ├── components/
│ │ └── Input.jsx
│ ├── App.jsx
│ ├── cities.json
│ ├── index.css
│ ├── main.jsx
├── tailwind.config.js
├── postcss.config.js
├── package.json
└── README.md
```

## 📦 Installation

```bash
# Clone this repo
git clone https://github.com/sadranafe/sadranafe-bootcamp.git

# Move into the project folder
cd sadranafe-bootcamp
cd week16-inputMask

# Install dependencies
npm install

# Start the development server
npm run dev
```